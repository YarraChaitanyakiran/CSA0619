#include<stdio.h>
int main()
{
	int i,n,arr[i];
	printf("Enter the number of elements: ");
	scanf("%d",arr[i]);
	for(int i=0;i<n;++i);
	if(arr[0]<arr[i])
	arr[0]=arr[i];
	printf("largest array element=%.2f",arr[0]);
	return 0;
}